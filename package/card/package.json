{
	"name": "@metacraft/engines-under-realm",
	"description": "core game algorithm for Under Realm - raise of magic",
	"version": "0.0.14",
	"main": "./dist/index.js",
	"types": "./dist/index.d.ts",
	"license": "MIT",
	"files": [
		"dist/**/**"
	],
	"scripts": {
		"build": "rm -rf ./dist && tsc",
		"release": "npm run build && npm publish --access public"
	},
	"devDependencies": {
		"typescript": "^4.5.3"
	}
}
